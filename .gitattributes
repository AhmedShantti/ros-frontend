# Normalise line endings in the repository to LF, whatever the checkout
# platform does locally. Without this, a Windows checkout stages CRLF and
# every file shows as modified for the next contributor on macOS or Linux.
* text=auto eol=lf
